export interface browsing {
  articleCount: string;
  articleToday: number;
  boardCount: number;
  boardToday: number;
  registerCount: number;
  registerToday: number;
  todayVisitor: number;
  visitorCount: number;
}

export interface newRegister {
  pageSize: number;
  pageNum: number;
}
